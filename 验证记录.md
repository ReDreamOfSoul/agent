# 代码验证记录

《机器人学导论》课程智能体（2025AI068）—— 本机实测验证结果（2026-09-04）。

## 环境

| 项 | 值 |
|---|---|
| 操作系统 | Windows（PowerShell） |
| Python | 3.14.7 |
| 本机办公软件 | Microsoft Office 16（Word / PowerPoint COM 可用） |
| 知识库 | 教材PDF（320页，扫描版OCR）+ 十章课件（10个PPT/PPTX）+ 题库.doc + 教学大纲.doc |

## 一、单元测试（7/7 通过）

```
python tests/test_workflow.py
PASS test_homework_exact          # 客观题精确批阅
PASS test_homework_keyword_partial  # 主观题要点评分
PASS test_intent_classifier       # 意图识别（作业/习题/背景/知识点）
PASS test_kb_retrieval_hit        # 知识库命中检索
PASS test_workflow_generic_fallback  # 未命中走通用兜底
PASS test_workflow_kb_generation  # 命中走知识库整合生成
PASS test_workflow_record_and_followup  # 交互记录 + 引导式追问
```

## 二、知识库构建（真实课程资料）

```
python scripts/build_kb.py --rebuild
```

- 教材《机器人技术基础 第3版》：320 页扫描版，无文字层 → RapidOCR 逐页识别（增量缓存 data/cache/）
- 十章课件（第一章~第十章）：PPT/PPTX 按页解析，旧版 .ppt（第七章）经 PowerPoint COM 转换成功
- 题库.doc（9 道题含答案）、教学大纲.doc：Word COM 提取入库
- 检索模式：未配置 Embedding 时自动降级为 BM25 关键词检索

## 三、接口实测（FastAPI + uvicorn）

| 接口 | 结果 |
|---|---|
| GET /health | 200 ok |
| POST /v1/chat | 知识库命中（得分 0.90+），返回 5 个来源、8 节点流程轨迹、引导式追问 |
| POST /v1/sessions | 创建会话成功 |
| POST /v1/homework/judge | 题库匹配→要点评分 0.82→批阅反馈（履带式机器人题目） |
| POST /v1/feedback | 反馈入库成功 |
| GET /v1/insight | 学情汇总（会话数/提问数/命中率/薄弱点/日活跃） |
| GET /v1/tools | 返回 4 个 MCP 工具 |

## 四、MCP 服务端实测（JSON-RPC over stdio）

```
initialize          → 协议握手成功（协议版本 2025-03-26）
tools/list          → 4 个工具（course_kb_retrieval / exercise_judge / study_record / graph_query）
tools/call          → course_kb_retrieval 真实命中（第三章机器人运动学课件，得分 1.0）
                      graph_query 返回前置知识点（第二章 机器人本体结构）
```

## 五、已知限制

1. **检索阈值（θ=0.72 / 关键词 0.5）为初始设计值**：未配置向量服务时为纯关键词模式，命中判定偏严，
   正式上线前应以标准测试集实测调优（config/config.yaml → retrieval）；
2. **教材 OCR 耗时**：320 页约 40-50 分钟（7-11 秒/页），已做增量缓存，中断可续跑；
3. **双图谱**：graph_query 当前基于课程章节顺序返回前置/后继关系，完整图谱数据建设后可替换；
4. **LLM 应答**：当前为 dummy 本地模拟后端（保证无密钥可全链路运行），配置 .env 后切换真实大模型。

---

# 追加：真实大模型联调与新模块验证（2026-09-05）

## 六、真实 LLM 接入（阿里云百炼兼容模式）

- `.env` 配置 `LLM_API_BASE_URL / LLM_API_KEY / LLM_MODEL` 后，`config.py` 自动将 provider 从 dummy 切换为 openai；
- 实测模型 qwen3.8-27b：小请求约 3s，大上下文（5 段×480 字 + 1024 max_tokens）单次生成约 37-56s；
- **适配调整**：`llm.timeout_seconds 60→180`、`retry_times 1→0`（避免超时重试翻倍等待）；
  LLM 上下文每段截断 320→480 字（块长 500 字，避免关键句被截断导致模型误判"片段无原文"）。

## 七、检索质量修复（真实问答暴露并定位）

| 问题 | 根因 | 修复 |
|---|---|---|
| 轨迹规划问答答错 | 习题块（"分为哪两类/有什么特点"句式）在覆盖率和排序中虚高，挤掉正文答案块 | 停用词扩充：特点/两类/各有/主要/简述/说明/区别/作用 等提问功能词 |
| 标点"？"参与匹配 | tokenize 未过滤标点符号 token | 增加中文/字母/数字有效性校验，剔除纯符号 token |

修复后检索实测：`机器人轨迹规划分为哪两类？各有什么特点？` → q_tokens 仅 `机器人轨迹规划`，
top5 含教材第五章正文答案块（"轨迹规划既可以在关节空间中进行，也可以在直角坐标空间中进行"），
真实 LLM 正确作答："关节空间轨迹规划 + 笛卡尔空间（作业空间）轨迹规划"，best_score=1.0。

## 八、真实 LLM 下全接口实测（2026-09-05）

| 接口 | 结果 |
|---|---|
| POST /v1/chat | 命中知识库 best=1.0，回答 3960 字；无关问题（量子计算）正确拒绝走通用兜底 |
| POST /v1/chat/stream（新增） | SSE 62 事件（meta→60×delta→done），前端打字机渲染 |
| GET /v1/exercises（修复） | 题库解析 8 题（真实"序号、题目…答案："格式，占位答案已过滤），limit=5 全题库题，AI 生成 0 |
| POST /v1/homework/judge | 履带题要点评分 0.61（keyword） |
| GET /v1/graph/overview、/v1/graph | 10 章总览；运动学前置=机器人本体结构 |
| GET /v1/self-assessment、/v1/recommend | 学习画像（提问/命中率/作业/薄弱点）；推荐 1 条学习建议 |
| GET /v1/insight | 学情汇总：命中率 0.8333 |
| GET /v1/export/{table} | CSV 导出 200（含 BOM，Excel 可直接打开） |
| GET / | 前端演示页 200（学生端 5 模块 + 教师端 4 模块） |

## 九、新增单元测试（tests/test_agent_modules.py，7/7 通过）

```
PASS test_exercise_extract_real_format  # 真实题库格式（\r换行/选项题/占位答案过滤）
PASS test_exercise_extract_cross_chunk  # 跨块截断合并解析
PASS test_exercise_extract_compat_format  # "题目：…参考答案："兼容格式
PASS test_graph_overview                # 10 章图谱总览
PASS test_graph_query                   # 前置/后继/学习目标/未匹配
PASS test_study_session_summary         # 学习画像聚合
PASS test_study_export_rows             # 明细导出与非法表名校验
```

全量回归：tests/test_workflow.py 7/7 + tests/test_agent_modules.py 7/7 均通过；
前端 index.html JS 经 node --check 语法校验通过（SSE 流式渲染改造）。

---

# 追加：视频课程功能（2026-09-05）

## 十、视频接入与功能设计

- 知识库 `data/knowledge_base/video/` 新增 3 个教学视频（ch06_1/947KB、ch06_2/27MB、ch09_1/16MB）；
- `scripts/gen_video_manifest.py`：扫描视频目录自动生成/更新 `manifest.json`（文件名 `ch{章}_{序号}` 自动推断所属章节，保留人工补充的标题/描述/时长）；
- `knowledge_base.parse_all` 读取清单生成「视频」类型知识块（resource_type=视频，携带 video_url），可被检索命中；
- `TextChunk` 新增 `video_url` 字段（默认空，兼容旧索引）；
- `main.py` 挂载 `/videos` 静态服务指向视频目录，前端 `<video>` 直接播放；
- 新增 `GET /v1/videos`：视频清单按章节分组；
- 前端：新增学生端「视频课程」面板（章节分组卡片 + 播放弹窗）；智能问答中命中视频块时在来源处显示「🎬 观看视频」；命中文本块时按章节自动匹配并推荐配套视频。

## 十一、视频功能实测（2026-09-05）

| 项 | 结果 |
|---|---|
| 索引重建 | 1496 + 3 视频块 = 1499 块 |
| GET /v1/videos | total=3，第六章 2 个、第九章 1 个 |
| 静态播放 | /videos/ch06_1.mp4(200/947KB)、ch06_2(200/27MB)、ch09_1(200/16MB) |
| 视频问答 | "第六章控制系统有没有配套的教学视频？" → 命中视频块（video_url 正确透传） |
| 文本问答回归 | "轨迹规划分为哪两类" 仍正确命中教材正文块（视频块未干扰排序） |
| JS 校验 | node --check 通过 |

---

# 追加：来源折叠与课程元信息（2026-09-05）

## 十二、来源信息折叠

- 对话回答下方的"知识点来源"列表（教材/课件来源、章节、页码、相似度、文本片段）默认折叠为一个下拉按钮：`📚 知识点来源（N）· 点击展开`，点击才展开详情；
- 课程信息类回答（intent=course_info）不显示知识点来源，改为显示 `📋 来源：课程教学大纲` 徽标；
- 流程轨迹仍保留独立折叠（原有 details）。

## 十三、课程元信息清单

- 从《机器人学导论》课程教学大纲（彭鹏，2022年7月）完整提取课程级信息，生成 `data/course_meta.json`：
  - 基本信息：课程名称（中/英）、课程代码 L7043、开课单位、适用专业、专业负责人姚光乐、课程负责人/任课教师彭鹏、学时32/学分2、课程性质开放选修课；
  - 教材：《机器人技术基础》刘极峰、杨小兰，高等教育出版社2019，ISBN 9787040526127；3本主要参考书；
  - 考核：课堂表现10% + 课程作业20% + 期末成绩70%（闭卷）；
  - 先修课程：高等数学、线性代数、控制理论及方法、C/C++程序设计；
  - 课程说明、课程目标（2条，含毕业要求支撑）、教学大纲十章（每章含学时/教学方式/知识点/学习目标/支撑目标）。
- `app/core/course_meta.py`：加载元信息，`summary(question)` 按问题关键词重点突出相关字段，`answer()` 复用 LLM 基于元信息生成回答；
- 意图识别新增 `course_info`（优先级最高，关键词：课程/教材/老师/学分/考核/先修/大纲/课程目标等），命中后**跳过知识库检索**，直接用课程元信息回答，避免污染知识点检索排序；
- 新增 `GET /v1/course/meta` 接口；
- 前端学生端新增「📋 课程信息」面板：基本信息卡片、教材与参考书、考核方式、课程目标、教学大纲十章表格。

## 十四、课程元信息实测（2026-09-05）

| 项 | 结果 |
|---|---|
| GET /v1/course/meta | 机器人学导论，10章，教师彭鹏，教材《机器人技术基础》 |
| "这门课用什么教材？" | intent=course_info，正确回答教材（名称/作者/出版社/年份/ISBN）+ 3本参考书，来源=课程教学大纲 |
| "任课老师是谁？" | intent=course_info，回答彭鹏，并补充专业负责人姚光乐 |
| "多少学分？考核方式？" | intent=course_info，回答2学分32学时 + 考核构成 |
| 知识点问答回归 | "轨迹规划分为哪两类" intent=knowledge，hit=True，来源5条（前端折叠） |
| 单测 | test_workflow 7/7 + test_agent_modules 7/7 |
| JS 校验 | node --check 通过 |

---

# 追加：回答精简与 LaTeX 公式修复（2026-09-05）

## 十五、问题定位

用户反馈：回答冗长，每条都带"依据：课程资料片段1中..."等套话；LaTeX 公式（`\[\text{课程总评成绩} = ...\]`）直接显示原始代码，无法渲染。

## 十六、修复方案

1. **LLM 提示词精简**（`app/core/llm.py`）：
   - System prompt：明确要求"简洁直接，只回答学生问的问题，不要说'根据资料/依据如下/课程资料片段中'等套话，不要列出参考来源，不要使用LaTeX公式语法，数学公式用普通文本表达"；
   - `generate_with_kb` / `generate_generic` user prompt 同步精简，去掉"并说明依据的知识点或章节"等要求。
2. **前端 MathJax 兜底渲染**（`app/static/index.html`）：
   - 引入 MathJax 3 CDN，配置 `\(...\)` 行内公式和 `\[...\]` 块级公式；
   - `renderBotMessage` 完成后调用 `MathJax.typesetPromise([container])`，即使模型偶尔输出 LaTeX 也能正常渲染；
   - 提示词已禁止 LaTeX，MathJax 仅作兜底。

## 十七、实测对比（2026-09-05）

| 问题 | 修复前 | 修复后 |
|---|---|---|
| "考核方式是什么？总评成绩怎么算？" | 3600+字，含"依据：课程资料片段1..."，LaTeX公式显示原始代码 | 129字，简洁直接，公式用普通文本"总评成绩 = 课堂表现×10% + 课程作业×20% + 期末成绩×70%" |
| "任课老师是谁？" | 含套话+补充信息，约100字 | 直接回答"彭鹏"（2字） |
| "什么是齐次变换矩阵？" | 较长，含来源说明 | 94字，简洁准确，仍正常命中知识库 |

含LaTeX检测：False | 含套话(依据/根据资料/课程资料片段)检测：False

---

# 追加：课程资料下载与资源归纳整理（2026-09-05）

## 十八、需求

用户要求智能体不只是回答问题，还能给出课程资料下载链接，如"给我一份第三章的PPT"、"有没有介绍机器人控制的视频"。需要完善知识库和相关资源的归纳整理。

## 十九、实现

1. **资源清单生成器**（`scripts/gen_resources.py`）：扫描知识库目录，自动整理 16 个课程资源：
   - 10 章课件（PPT/PPTX），按文件名推断章节；
   - 教材 PDF（全局）；
   - 题库 DOC（type=exercise）；
   - 教学大纲 DOC（type=syllabus）；
   - 3 个教学视频（复用 video/manifest.json）。
   输出 `data/resources.json`，每个资源含 id/title/type/chapter/file/url/description。

2. **资源管理器**（`app/core/resource_manager.py`）：
   - 加载资源清单；
   - `extract_chapter(question)`：从问题中提取章节（"第三章"→第三章，"运动学"→第三章，"机器人控制"→第六章）；
   - `extract_types(question)`：提取资源类型（PPT/视频/教材/题库/大纲）；
   - `match(question)`：按 章节+类型 > 仅章节 > 仅类型 > 全局资源 优先级匹配；
   - `format_answer()`：直接格式化输出资源列表（不依赖 LLM，确保下载链接准确）。

3. **意图识别**（`app/core/workflow.py`）：
   - 新增 `resource` 意图（关键词：资料/课件/PPT/下载/给我一份/有没有/题库/学习材料等）；
   - **资源请求动词前置判断**：含"下载/给我/有没有/发我/传我/帮我找"等动词时直接判 resource，避免被 course_info 截走（如"给我教材"不再误判为课程信息查询）；
   - 优先级：course_info（无资源动词时）> resource > homework > exercise > knowledge。

4. **resource 回答分支**（`app/core/workflow.py`）：
   - 命中 resource 后跳过知识库检索，直接调用 ResourceManager 匹配资源；
   - sources 携带 `resource_url` 和 `resource_type`，供前端渲染下载/播放按钮；
   - 不生成追问，不记录学情画像（资源请求非知识点学习）。

5. **静态文件服务**（`app/main.py`）：
   - 挂载 `/resources` → `data/knowledge_base/`，所有 PPT/教材/题库/大纲可通过 `/resources/文件名` 下载；
   - 视频已有 `/videos` 挂载。

6. **前端**（`app/static/index.html`）：
   - sources 渲染新增 `resource_url` 识别：视频类型显示"▶ 播放视频"按钮，其他类型显示"📥 下载PPT/PDF/文档"按钮；
   - resource 意图的 sources 不折叠（直接显示下载按钮），其他意图仍折叠在"📚 知识点来源"下拉框。

## 二十、实测（2026-09-05，真实 LLM qwen3.8-27b）

| 问题 | intent | 结果 |
|---|---|---|
| 给我一份第三章的PPT让我下载学习 | resource | 返回第三章 机器人运动学（课件），下载链接 /resources/第三章 机器人运动学.pptx ✓ |
| 有没有介绍机器人控制的视频？ | resource | 返回第六章 2 个教学视频，播放链接 /videos/ch06_1.mp4、ch06_2.mp4 ✓ |
| 给我教材 | resource | 返回教材 PDF 下载链接（修复前误判 course_info 只给文字）✓ |
| 给我题库 | resource | 仅返回题库下载链接（修复前返回3个全局资源）✓ |
| 给我教学大纲 | resource | 返回教学大纲下载链接 ✓ |
| 有没有运动学的课件？ | resource | 返回第三章 PPT ✓ |
| 这门课用什么教材？ | course_info | 回归正确：只回答教材信息，不触发下载 ✓ |
| 什么是齐次变换矩阵？ | knowledge | 回归正确：正常命中知识库 ✓ |

资源清单总计 16 个：10 PPT + 1 教材PDF + 1 题库 + 1 大纲 + 3 视频。
